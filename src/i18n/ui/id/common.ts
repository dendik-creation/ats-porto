import type { CommonStrings } from '../../ui.types';

const common: CommonStrings = {
  skipToContent: 'Langsung ke konten',
  present: 'Sekarang',
  country: { ID: 'Indonesia' },
};

export default common;
